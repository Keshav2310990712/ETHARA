{
  "name": "ethara-project-manager",
  "version": "1.0.0",
  "description": "Premium ERN + SQL Project and Task Management Application",
  "main": "backend/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm start --prefix backend",
    "backend-dev": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend-dev\" \"npm run frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
